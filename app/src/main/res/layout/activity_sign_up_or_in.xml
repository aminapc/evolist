<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_sign_up_or_in"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/back"
    tools:context="com.evoteam.evolist.SignUpOrInActivity">

    <TextView
        android:textColor="#595858"
        android:text="©Licensed by Franky and Sanjii"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="29dp"
        android:id="@+id/licenseTextView"/>

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:srcCompat="@drawable/applogo"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/licenseTextView"
        android:layout_alignStart="@+id/licenseTextView"/>

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/login"
        android:background="@drawable/bg"
        android:id="@+id/signInImageButton"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"/>

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/signup"
        android:id="@+id/signUpImageButton"
        android:background="@drawable/bg"
        android:layout_below="@+id/signInImageButton"
        android:layout_alignLeft="@+id/signInImageButton"
        android:layout_alignStart="@+id/signInImageButton"/>

</RelativeLayout>
